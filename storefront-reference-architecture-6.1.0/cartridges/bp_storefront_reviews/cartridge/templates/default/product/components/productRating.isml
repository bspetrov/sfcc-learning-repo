<isloop items="${[1, 2, 3, 4, 5]}" var="star">
    <i class="fa
        <isif condition="${pdict.avgProductGrade >= star}">
            fa-star
        <iselseif condition="${(pdict.avgProductGrade % 1 > 0) && (Math.ceil(pdict.avgProductGrade) >= star)}">
            fa-star-half-o
        <iselse>
            fa-star-o
        </isif>
        " aria-hidden="true">
    </i>
</isloop>